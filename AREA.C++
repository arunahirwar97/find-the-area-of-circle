
    #include< iostream.h>
    #include< conio.h>
    void main()
       {
            float  area,r,pai=3.414;
            cout<<"Enter the radius of circle::";
            cin>>r;
            area = pai*r*r;
            cout<<"Area of given radiuce is : "<< area;
            getch();
        }  
